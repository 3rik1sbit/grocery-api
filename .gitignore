# Dependencies
/node_modules

# Data
/database.json
# ...and the copies server.js writes before it rewrites the file. They are
# the same data with a timestamp, so they are ignored for the same reason.
/database.json.bak-*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2-*.log

# Environment variables (if you add them later)
.env
